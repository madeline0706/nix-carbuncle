// Command manifest.
//
// Import each command module here so it registers itself. Bare terminal ships
// with no commands, so this list is intentionally empty. Add commands like:
//
//   import './whoami.js';
//   import './help.js';
//
// Each of those files does `register({ name, desc, execute })` from ./registry.js.
